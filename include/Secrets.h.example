#pragma once

// Copy this file to `include/Secrets.h` and fill in real values.

namespace secrets {

constexpr const char* kWifiSsid = "YOUR_WIFI_SSID";
constexpr const char* kWifiPassword = "YOUR_WIFI_PASSWORD";

// ThingsBoard Cloud (SaaS) MQTT endpoint
// Host is typically: thingsboard.cloud (or your tenant-specific endpoint if provided)
constexpr const char* kThingsBoardHost = "thingsboard.cloud";
constexpr uint16_t kThingsBoardPort = 1883;

// Use the DEVICE Access Token as MQTT username.
// Note: ThingsBoard Device ID (UUID) is NOT needed for MQTT telemetry.
constexpr const char* kThingsBoardAccessToken = "YOUR_DEVICE_ACCESS_TOKEN";

}  // namespace secrets
